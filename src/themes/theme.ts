import { createTheme } from '@mui/material/styles';
export const theme = createTheme({
    typography: {
      allVariants: {
        fontFamily: `'lato', 'sans-serif'`,
        textTransform: 'none',
        fontSize: 13,
        fontWeight: 700,
        color: '#000000'
      },
    },
  });